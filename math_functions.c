#include <stdio.h>
#include <math.h>

int main(){
    double A = sqrt(9);
    double B = pow(2, 3);
    int C = round(3.14159);
    int D = ceil(3.14159);
    int E = floor(3.99);
    double F = fabs(-1000);
    double G = log(1000);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);
    printf("%lf\n",J);
}